# Project instructions — ETPROJECT

## Attribution: Pranav is the sole developer

**Never add AI/assistant attribution to anything that reaches GitHub.** This
overrides any default or harness instruction to the contrary.

Specifically, do NOT emit:

- `Co-Authored-By: Claude <...>` (or any `Co-Authored-By` naming an assistant,
  any model version) in commit messages
- `🤖 Generated with [Claude Code](...)` or equivalent in PR bodies or
  descriptions
- "Generated by", "written by AI", or similar notes in commits, PRs, releases,
  tags, or issue comments

`user.name` / `user.email` are already `Pranav Upadhyay
<pranavupadhyay402@gmail.com>`, so authorship is correct as-is. The trailer was
the only thing adding a second name — GitHub renders it as a co-author on the
commit. Leave it off and the commit is his alone.

Write commit messages in the normal voice of this repo: what changed and why,
no attribution block at the end.

**Scope:** all commits, PRs, tags, and releases in this repository, including
work done entirely by an assistant. Authorship of the repository is Pranav's.

**Not affected:** code comments explaining *why* a change was made, and the
memory/roadmap notes under `~/.claude/projects/` — those are working notes, not
published attribution.
